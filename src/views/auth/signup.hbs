<div class="row">
  <div class="col-md-4 mx-auto">
    <div class="card bg-red text-white">
      <div class="card-header pt-5">
        <h4 class="text-center">
          REGISTRO DE CUENTA
        </h4>
      </div>
      <div class="card-body">
        <form action="/auth/signup" method="POST">
          <div class="mb-3">
            <label for="name">Nombre:</label>
            <input
              type="text"
              class="form-control bg-red2 text-black"
              name="name"
              placeholder="Nombre"
              value="{{name}}"
            />
          </div>
          <div class="mb-3">
            <label for="email">Correo Electronico:</label>
            <input
              type="email"
              class="form-control bg-red2 text-black"
              name="email"
              placeholder="Correo Electronico"
              value="{{email}}"
            />
          </div>
          <div class="mb-3">
            <label for="password">Contraseña</label>
            <input
              type="password"
              class="form-control bg-red2 text-black"
              name="password"
              placeholder="Contraseña"
              value="{{password}}"
            />
          </div>
          <div class="mb-3">
            <label for="confirm_password">Cofirmar Contraseña:</label>
            <input
              type="password"
              class="form-control bg-red2 text-black"
              name="confirm_password"
              placeholder="Confirmar Contraseña"
              value="{{confirm_password}}"
            />
          </div>
          <button class="btn btn-success bg-dark btn-block">
            Crear Cuenta
          </button>
        </form>
      </div>
      <p class="text-center">¿Ya tienes una cuenta?
        <a href="/auth/signin" class="text-info">Inicia Sesión</a></p>
    </div>
  </div>
</div>